{% extends 'base.html.twig' %}

{% block title %}controller_name{% endblock %}

{% block body %}
	<h1 class="h1 my-4 font-weight-normal text-center">{{controller_name}}</h1>
    <div>
		<a href="{{ path('profile_index') }}" class="mt-2 btn text-primary">Retour</a>
	</div>
{% endblock %}