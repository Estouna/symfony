{% extends 'base.html.twig' %}

{% block title %}Administration{% endblock %}

{% block body %}
<h1 class="h1 my-4 font-weight-normal">{{ controller_name }}</h1>

{% for message in app.flashes('message') %}
    <div class="col-6 text-center">
        <div class="p-3 my-2 rounded-1 alert alert-success">{{message}}</div>
    </div>
{% endfor %}

<div class="d-flex flex-column col-lg-5 text-center">

	<a href="{{ path('admin_articles_add') }}" class="mt-5 mb-2 btn btn-success">Publier un article</a>

    <a href="{{ path('admin_articles_index') }}" class="mt-5 mb-2 btn btn-info">Gestion des articles</a>

    <a href="#" class="my-2 btn btn-info">Gestion des commentaires</a>

    <a href="#" class="my-2 btn btn-info">Gestion des utilisateurs</a>
</div>


{% endblock %}
